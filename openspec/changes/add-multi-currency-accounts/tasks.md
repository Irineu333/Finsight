> **A ordem tem uma alavanca só: a segunda moeda nasce exclusivamente no seletor do formulário
> de conta/cartão.** Por isso ele é a **última** tarefa do plano. Antes dele, todo mapa por moeda
> tem uma chave, toda figura é exata e nenhuma marca `≈` aparece — a janela de "grava moeda mas
> soma moedas diferentes" não é estreitada, é **inalcançável**.
>
> E isso não é sustentado por disciplina: 1.6 escreve um **teste de inércia** afirmando que
> nenhum sítio de produção constrói conta com moeda diferente do padrão. Ele vale de 1.6 até
> 9.7, e 9.8 o **inverte** em vez de removê-lo, passando a valer para sempre como "exatamente
> dois sítios escolhem moeda".
>
> **Regra de higiene, e ela é o que corrige o erro mais fácil de cometer aqui:** nenhum grupo
> muda assinatura sem adaptar, no mesmo grupo, quem a consome. O build fica verde ao fim de
> **todo** grupo, e 3.9 + 3.10 são o gate que prova, a cada um, que o usuário monomoeda vê números
> idênticos aos de hoje **e** que uma conta de moeda diferente da base é exibida na moeda dela.
> Os dois são necessários: 3.9 sozinho é cego a confundir a moeda da conta com a base, porque para
> um usuário monomoeda os dois textos coincidem.
>
> §1 é varredura preparatória e inerte, e produz o inventário de superfície que os grupos 6–8
> consomem — andaime da implementação, apagado por 10.6 quando 8.10 provou a lista vazia.
> §4 e §5 não dependem de §2 nem de §3 e podem correr em paralelo.

## 1. Varreduras preparatórias (comportamento idêntico, nada de semântica)

- [x] 1.1 (D10) `DisplayAmount` passa a carregar `currency` **obrigatório** e a exatidão, sem default em nenhum dos 7 construtores nomeados — a spec proíbe expor um sem os outros. Atualizar os sítios de construção; onde o sítio já tem a `Account`, passar `account.currency`; onde agrega entre contas, passar **explicitamente** a moeda única que o app tem neste ponto do plano — que em 6.6 passa a ser a moeda base. Reescrever o KDoc que hoje declara que o tipo não conhece moeda, com a distinção da spec: carregar a denominação não é calcular.
- [x] 1.2 Registrar, como saída de 1.1, a **lista dos sítios que tiveram de passar a moeda explicitamente** por agregarem entre contas. Essa lista é o backlog verificável dos grupos 6–8, e 8.6 a fecha vazia. Sem ela a varredura de superfície não tem critério de pronto. **Registrada em `inventory-assumed-single-currency.md`**, nesta mesma pasta, com a constante marcadora `ASSUMED_SINGLE_CURRENCY` (`:core:model`) tornando-a greppável em vez de mantida à mão.
- [x] 1.3 `CurrencyFormatter.format`/`formatWithSign` passam a receber a moeda nos três `actual` (jvm, android, ios); o locale governa apenas separador e posição do símbolo. Decidir aqui — e registrar no KDoc — se a moeda entra pelo método ou muda o binding `single { CurrencyFormatter() }` em `CommonModule`. **A decisão precede a varredura de 8.x**; tomá-la no meio dela é retrabalho contratado. Verificável: não sobrevive sobrecarga que formate sem moeda.
- [x] 1.4 `CurrencyFormatter.format(DisplayAmount)`: o `≈` como prefixo **mais externo** que o sinal (`≈ +R$ 1.240,00`), pela mesma porta que já concatena `+`/`-`. Este ponto único conserta a maioria dos sítios de formatação sem tocá-los. Inerte enquanto nada for aproximado. Cobrir em `DisplayAmountTest`.
- [x] 1.5 `LedgerFixture` (`core/ledger/jvmTest`) ganha parâmetro de moeda em conta e em entry, com default `"BRL"` para que as 6 suítes construídas sobre ela compilem sem alterar asserção. É `internal` ao `core/ledger/jvmTest`, então serve às suítes de query (3.8) e **não** ao `LedgerEntryWriterTest`, que mora em `feature/transactions/impl/commonTest` e precisa dos seus próprios doubles (2.8).
- [x] 1.6 **Teste de inércia** em `app/shared/jvmTest`, ao lado de `AppModulesTest`: nenhum sítio de produção constrói conta com moeda diferente da única que o app usa. É o que torna a janela de silêncio inalcançável durante os grupos 2–8, e 9.8 o inverte.
- [x] 1.7 Reordenar `PayInvoiceModal` e `AdvancePaymentModal` para *quem participa → quanto → quando* (D24). Sem campo novo e sem mudança de comportamento. Isolada aqui de propósito: é a mudança de UX de maior alcance da change, atinge quem nunca verá duas moedas, e precisa ser revisável e reversível sozinha.

## 2. O tipo de conta e a fronteira de escrita

> §1 e §2 podem ser um único PR. O que **não** pode é 2.1 sem 2.2: acrescentar um membro a
> `AccountType` sem fechar `systemAccountId` deixa um `when`-expressão não exaustivo, e
> `:core:ledger` não compila.

- [x] 2.1 `AccountType` ganha `CONVERSION` — `isDebitNatured = false`, `isMonetary = false`, `isNominal = false`, `isPermanent = true` **vacuamente** (a propriedade decide se arquivar encalha saldo, e conversão nunca é arquivada). `AccountEntity.Type` idem, com KDoc registrando o fato verificado de que **não há migração**: Room persiste o enum nativamente como `TEXT`, sem `TypeConverter`. `SystemAccount` ganha a chave de conversão. `displaySign` não muda — deriva de `isDebitNatured`.
- [x] 2.2 Fechar os **três** `when` exaustivos sobre `AccountType` do repositório: `AccountTypeMapper:13` e `:21`, e `LedgerEntryWriter.systemAccountId:158`. Não há uso de `AccountType.entries`/`values()`.
- [x] 2.3 Provar D2 em `LedgerTest`: `{ASSET, CONVERSION} → TRANSFER`, `{ASSET, LIABILITY, CONVERSION} → PAYMENT`, e `deriveTransactionType` de uma perna monetária de operação cruzada **não** lendo `ADJUSTMENT`. Nenhuma mudança de produção é esperada — se falhar, o *fall-through* está errado e é isso que se conserta.
- [x] 2.4 Provar, também sem mudança de produção, que o gate de editabilidade (D19) recusa a operação cruzada por contagem de pernas monetárias, e que a idempotência de `AdjustBalanceUseCase`/`AdjustInvoiceUseCase` não casa com uma transação de pernas `CONVERSION`. São os dois comportamentos que a revisão apontou como quebráveis, e a prova de que o tipo próprio os salvou.
- [x] 2.5 (D3, D4) `AccountDao.getByTypeAndName` passa a `(type, name, currency)` — **uma conta de sistema por moeda, não por par**; `ensureSystemAccount` cria por moeda; `systemAccountId` resolve por `(SystemAccount, currency)` — a natureza deixou de ser chave, porque `EQUITY` passa a ter duas contas de sistema por moeda. As contas de sistema existentes, todas `'BRL'`, viram as contas de sistema **de BRL**, sem migração.
- [x] 2.6 `writeEntries` grava `currency` lida da `AccountEntity` que `orRejectIfClosed` já carrega, nas duas construções de `EntryEntity` e na contrapartida. `TransactionLeg` **não** ganha campo — é o que torna "poste 100 USD numa conta BRL" inexprimível. O default do modelo permanece por ora e sai em 6.6, junto do resolvedor que o substitui.
- [x] 2.7 (D7) **Remover** `LedgerEntryWriter.validate(legs)` e as suas duas chamadas em `TransactionRepository:219` e `:240`, e os dois testes que a exercem. Consertá-la é impossível sem quebrar D5 — agrupar por moeda exigiria ler contas —, e a invariante volta a ter um ponto único de fato.
- [x] 2.8 (D1, D6, D15) Completação cruzada em `writeEntries`: agrupar por moeda, e com duas ou mais lançar o oposto do resíduo de cada uma na conta `CONVERSION` daquela moeda — **última calculada, por diferença, e sem dimensão**. Sem a ausência de dimensão, `rejectIfDimensionLandsWrong` recusa todo pagamento de fatura cruzado. Monomoeda permanece byte a byte o comportamento de hoje. Guarda de D1 (resíduos não todos do mesmo sinal) como caso novo de `LedgerError`, com `toUiText()` e strings em `values/` **e** `values-en/`.
- [x] 2.9 Testes em `LedgerEntryWriterTest`, com os seus próprios doubles cientes de moeda: transferência cruzada com 4 entries somando zero em cada moeda; pagamento de fatura cruzado com a conversão sem dimensão; resíduo de arredondamento absorvido pela perna de conversão; monomoeda sem conversão sintetizada; desbalanceamento monomoeda ainda recusado; resíduos de mesmo sinal recusados sem gravar nada.

## 3. As leituras por moeda, com os seus consumidores

> Grupo grande de propósito: mudar assinatura de `IEntryRepository` sem adaptar quem a consome
> deixaria o build vermelho por vários grupos. Ele fecha verde.

- [x] 3.1 (D8) Tipo de saldo por moeda em `:core:ledger`, com `zero`, `of(currency, value)` e acesso ao valor de uma moeda — as conveniências existem para que 3.7 seja substituição mecânica e não 21 decisões de desenho.
- [x] 3.2 Dar a esse tipo a **soma de dois resultados por moeda** (cada moeda com a sua, sem conversão), como implementação única e no razão. É o dono que `ledger-reporting` exige e que `dashboard-balance-widgets` consome; sem ele a operação nasce em linha dentro de `BalanceOverviewFactory:93`. Testes com moedas disjuntas e mapa vazio.
- [x] 3.3 (D8) `EntryDao`: aplicar `GROUP BY e.currency` pelo **critério**, não por lista — *toda agregação que não filtre por uma única conta*. Permanecem escalares apenas `balanceOf`, `balanceUpToMonth` e `accountPeriodTotals`. **Toda leitura por dimensão entra**, `dimensionNaturalBalance` e `dimensionPeriodTotals` inclusive: o razão MUST NOT consultar `DimensionKind` para decidir forma de retorno.
- [x] 3.4 Teste de regressão sobre os predicados por **literal SQL** da `EntryDao` e do `AccountDao` (`a.type = 'EQUITY'`, `IN ('ASSET','LIABILITY')`): nenhum passa a casar `CONVERSION`, e `netWorthCents` continua filtrando `ASSET`/`LIABILITY`. O compilador não os alcança, e o design os aponta como o risco real da abertura do enum.
- [x] 3.5 `IEntryRepository` e `EntryRepository`: novas assinaturas pelo mesmo critério de 3.3. `AccountFlows` mantém a forma e ganha a moeda; `DimensionFlows`, `dimensionOwed` e as suas versões em lote passam a por moeda.
- [x] 3.6 Decidir e registrar o destino de `netWorth()`: medido **sem consumidor de produção** — o saldo total do dashboard vem de `CalculateBalanceUseCase(accountId = null)`. Ou ganha consumidor, ou sai da interface. Verificável: não sobra assinatura sem chamador. **Decidido: saiu da interface.** Não havia um só chamador de produção — só stubs de teste que a recusavam. A query `netWorthCents` do `EntryDao` permanece, agora por moeda, porque as suítes de paridade de migração e de predicados a consultam direto; se um consumidor de produção aparecer (a figura de patrimônio de §5), ele a alcança pelo razão sem que a interface tenha carregado uma assinatura sem dono no meio do caminho.
- [x] 3.7 Varrer os ~21 arquivos de teste que implementam `IEntryRepository` como stub completo. Avaliar antes extrair uma base compartilhada — hoje cada um redeclara a interface inteira. Nenhuma expectativa muda: toda moeda é BRL. **Avaliado:** uma base *compartilhada entre módulos* exigiria módulo novo e plugin de convenção (`java-test-fixtures` não existe em KMP), aresta nova no grafo de todo `api`/`impl`, no meio da change — rejeitado. Adotada uma base **por módulo** (`StubEntryRepository`, no `commonTest` de accounts/categories/creditcards/dashboard/report, os cinco com ≥3 stubs), que recusa toda leitura não estubada em vez de responder zero; nos módulos de um stub só, correção mecânica. Reavaliar o módulo compartilhado quando um segundo contrato de fixture além de `IEntryRepository` pedir o mesmo.
- [x] 3.8 Casos de duas moedas nas 6 suítes de query sobre `LedgerFixture` (habilitadas por 1.5): cada agregação devolve duas chaves e nenhuma soma entre elas. É a prova executável de "nenhuma agregação soma moedas".
- [x] 3.9 **Teste de regressão monomoeda**, e ele é o gate de todos os grupos seguintes: com todas as contas na moeda base, toda figura do app é idêntica à de antes da mudança e nenhuma recebe marca de aproximação. Escrito aqui, rodado ao fim de cada grupo. **Mora em `CurrencyDenominationGateTest` (`core/database/jvmTest`)**, sobre banco e repositório reais: é onde toda figura do app nasce, já que toda figura é `Σ entries`.
- [x] 3.10 (D29) **Gate irmão de 3.9, e ele cobre o ponto cego dele:** teste com uma conta cuja moeda **difere da base**, provando que toda figura monomoeda carrega a moeda da sua conta ou fachada — saldo, extrato, devido de fatura, parcela — e **nunca** a base. Incluir o caso do usuário com **todas** as contas fora da base, **com taxa cadastrada**: nenhuma figura é convertida e nenhuma recebe marca, porque não havia mais de uma moeda a reconciliar (D9). Com moedas iguais a violação é invisível, porque os dois textos coincidem; 3.9 passaria com a base ligada por engano num saldo de conta. Como 1.1 instrui a passar a base nos sítios que agregam, este é o teste que separa os dois usos. **Irmão do 3.9 no mesmo arquivo.** A metade "com taxa cadastrada" fica para 5.5, que é quando a tabela de taxas existe (§4) — aqui o que se prova é que nenhuma leitura consulta taxa alguma. A repetição no nível de superfície é 7.8.
- [x] 3.11 Adaptar os consumidores de contas, categorias e orçamentos: `CalculateBalanceUseCase` (o `accountId = null` é a porta pela qual o multimoeda entra no app), `AccountsViewModel`, `CalculateCategorySpendingUseCaseImpl` e `ViewCategoryViewModel` (incluindo o denominador de porcentagem), `CalculateBudgetProgressUseCase` (`:feature:budgets:api`), que passa a reduzir o gasto à **moeda do limite** e não à base.
- [x] 3.12 Adaptar os consumidores de cartões: `CalculateInvoiceUseCase`, `CalculateInvoiceOverviewsUseCase`, `InvoiceTransactionsViewModel`. A redução a uma chave acontece **aqui**, com a garantia da fachada escrita onde o mapa é reduzido — não presumida no razão.
- [x] 3.13 Adaptar `DashboardComponentsBuilder` e `BalanceOverviewFactory` para somar `ASSET + LIABILITY` e `asset.expense + liability.expense` pela soma de 3.2, nunca por soma de mapas em linha.
- [x] 3.14 Adaptar `feature/report/impl`: `CalculateReportStatsUseCase` (escopo vazio = todas as contas, arquivadas inclusive — a figura mais cruzada do app), `CalculateReportCategorySpendingUseCase`, `ReportViewerViewModel`.
- [x] 3.15 `TransactionsViewModel` e o escopo: o perímetro decide por pernas **monetárias**, e as de conversão, fora de qualquer perímetro, não tornam fluxo um lançamento interno. Cobrir os cenários novos de `transaction-scope`.

## 4. A tabela de taxas (independente de §2 e §3)

- [x] 4.1 (D11) `ExchangeRateEntity(currency, date, rate, source)` e `ExchangeRateDao` em `:core:database`, com `source` distinguindo colhida-de-operação de informada-pelo-usuário; a consulta "última taxa em ou antes de `:date`" com precedência da do usuário na mesma data; a listagem observável.
- [x] 4.2 `AppDatabase` `version = 10` → `11` com `MIGRATION_10_11` registrada em `Database.kt`: `CREATE TABLE` da tabela de taxas **e** reconstrução de `budgets` com a coluna da moeda do limite (D13) — `ADD COLUMN` numa coluna `NOT NULL` exigiria um `DEFAULT` em SQL que nenhuma coluna deste schema tem; a reconstrução preserva `budget_categories` explicitamente, em vez de depender de a checagem de chave estrangeira estar ligada, preenchida com a moeda da **conta padrão** (`COALESCE` na constante de último recurso, se não houver). O preenchimento é **exato, não aproximado**: todo banco existente está inteiramente em BRL, então a moeda que a coluna recebe é exatamente a que já denominava cada limite gravado. Nenhum valor é alterado. Bindar o DAO em `databaseModule`.
- [x] 4.3 Exportar `schemas/…/11.json`; escrever `Migration10To11Test` no molde dos existentes e estender `MigrationSchemaEquivalenceTest`, hoje um `@Test` só cobrindo `7 → 10` — passa a validar a cadeia inteira `7 → 11`, e o `MigrationLedgerReadParityTest` com ela.
- [x] 4.4 Confirmar por teste que `LedgerBalanceCheck` **não muda** — já agrupa por `(transactionId, currency)` — com um banco contendo transação cruzada.

## 5. Consolidação e catálogo (depende de §4; independente de §3)

- [x] 5.1 Repositório de taxas com a política "a última em ou antes da data" e a precedência da taxa do usuário. **Interface em `:core:model`, implementação em `:core:database`** ao lado do DAO — a camada é consumida por cinco features, então não pode morar num `impl`; o molde é o do `EntryRepository`, que vive junto do seu DAO. Testes: taxa anterior escolhida em vez da posterior, ausência de taxa, precedência.
- [x] 5.2 Catálogo curado de moedas de duas casas decimais em `:core:model`, com a premissa de D14 em KDoc. O razão persiste só o código.
- [x] 5.3 A figura **multitermo** como sequência de `DisplayAmount`, com o caso de um termo como o comum, sem expor operação entre dois valores.
- [x] 5.4 A **única** redução de saldo-por-moeda a figura na base: **uma moeda passa direto, na sua própria moeda, exata** — a redução só age com duas ou mais; a partir de duas, converte o que a taxa da data permitir, deixa termo próprio por moeda sem taxa, e **deriva** a exatidão. Nada vira `1`, nada é omitido, nada zera a tela.
- [x] 5.5 Testes da redução contra as cinco linhas da tabela de D9 — incluindo **moeda única diferente da base com taxa cadastrada, que não converte** —, mais o de que não existe forma de obter a figura sem a exatidão, mais o de fronteira: a camada não expõe soma de dois saldos por moeda (isso é do razão, 3.2) e o razão não recebeu dependência que forneça taxa.

## 6. Moeda base e a feature de configurações

- [x] 6.1 (D18) Criar `:feature:settings:api` e `:impl` sob os plugins de convenção, com `include` em `settings.gradle.kts`, **`export` no framework iOS de `app/ios`** e a dependência em `:app:shared`. Sem o export o alvo iOS não compila.
- [x] 6.2 Rota `@Serializable`, `SettingsGraph`, `NavGraphBuilder.settingsGraph()`, módulo Koin e entry point; agregar em `AppNavHost` e `appModules`; entrada em `AppNavCatalog` (`feature/shell/impl`) imediatamente **antes** de `Support`. **Sem entry point**, pelo precedente de `feature/support/api`, que também não tem: um entry point agrupa a superfície de *UI* que outra feature consome, e o único acesso cross-feature a configurações é navegação — `SettingsRoute` e `ExchangeRatesRoute` na `api` (a segunda porque o rodapé de 7.9 navega direto para as taxas, de dentro de outra feature). Criar a interface vazia agora seria ruído; ela nasce quando houver o primeiro método com dono.
- [x] 6.3 Moeda base como preferência **observável**, no molde de `DashboardPreferencesRepository` — toda figura consolidada reage à sua mudança.
- [x] 6.4 Resolvedor de moeda pelo **locale do dispositivo** (`expect`/`actual` nos três alvos), reusando o mecanismo que o `CurrencyFormatter` já emprega — `NumberFormat.getCurrencyInstance()` no JVM/Android e `NSLocale.currentLocale` no iOS. Se a moeda do locale não estiver no catálogo de 5.2, recair na constante declarada como **último recurso**. Testes por plataforma, incluindo o caso de locale de moeda não oferecida.
- [x] 6.5 Resolver a moeda base **uma vez**, na primeira execução, a partir de 6.4, e persistir. Alteração posterior do locale MUST NOT alterá-la. Cobre também o app já instalado, onde `EnsureDefaultAccountUseCase` retorna cedo por já existir conta: a base é resolvida na ausência do valor persistido, e não na criação de conta.
- [x] 6.6 **Remover o default de moeda do modelo** (D28): `Account.currency` e `AccountEntity.currency` deixam de ter valor padrão, de modo que nenhuma conta seja construível sem que alguém decida a sua moeda — o compilador cobra. `BASE_CURRENCY` sai de `:core:ledger` e passa a ser a constante de último recurso da camada de consolidação. Remover o default do Kotlin é **neutro no schema**: um default de construtor não emite `DEFAULT` em SQL e a coluna segue `NOT NULL` — confirmar contra o `10.json` exportado.
- [x] 6.7 **Reetiquetagem das contas legadas** (D30): passo único de app, **após 6.4, 6.5 e 6.6** (o default do modelo já removido), que troca a moeda das contas existentes pela do locale quando ela difere da constante legada e está no catálogo. É `UPDATE` de denominação — nenhum valor, entry ou saldo é alterado. **Roda em uma única `withTransaction`** que grava o snapshot (6.7.1), aplica o `UPDATE` e persiste a flag "rodou" — crash entre passos deixa o banco no estado anterior, sem estado parcial. Registrar que rodou, para que troca posterior de região não redispare. Testes: banco legado em região estrangeira é reetiquetado e `LedgerBalanceCheck` continua limpo; região de origem não é tocada; moeda fora do catálogo não dispara; segunda execução não repete; **crash simulado após o `UPDATE` e antes de gravar a flag deixa tudo no estado pré-migração** (a `withTransaction` reverte). **Registrar no KDoc a consequência aceita** — o brasileiro de dispositivo em região estrangeira é reetiquetado sem aviso e sem desfazer. **A flag desceu para o banco** (`app_migration_log(step, migratedAt)`, criada na mesma migração 10→11): em `Settings` ela ficaria fora da `withTransaction`, e o crash entre o `UPDATE` e a flag deixaria o banco reetiquetado com o app achando que nunca rodou — o teste pedido nem seria escrevível. A leitura de "já rodou?" acontece **dentro** da mesma transação, e `step` é chave primária, de modo que a escrita é a própria reivindicação.
- [x] 6.7.1 (D30) **Snapshot pré-relabel** para dar rastro à migração irreversível: gravar, dentro da mesma `withTransaction` de 6.7, o par `(accountId, moeda anterior)` de cada linha que será tocada, numa tabela dedicada `account_currency_relabel_log(accountId, previousCurrency, newCurrency, migratedAt)`. Não é rollback automático — é auditoria consultável pelo suporte quando um usuário reclamar de figura em moeda inesperada. Migration cria a tabela na mesma versão do schema de §5. Teste: o log tem exatamente uma linha por conta reetiquetada, e nenhuma quando a região é a de origem.
- [x] 6.8 Ajustar os sítios que o compilador apontar em 6.6, cada um informando a moeda explicitamente: `EnsureDefaultAccountUseCase` lê a base; as previews de dashboard e as fixtures passam a sua; e os sítios de 1.2 passam a moeda base em vez da constante.
- [x] 6.9 Tela de configurações e tela de taxas: lista por moeda, data **sempre visível**, procedência no idioma de `CategoryCard:58-75` (`SwapHoriz` colhida / `ModeEdit` digitada) e sinalização de desatualizada aos 30 dias com cor `Warning` **e a palavra**.
- [x] 6.10 Modal de edição de taxa: campo numérico, `DatePickerModal`, e a sugestão externa como **placeholder** — único ponto do app onde rede é permitida, sem estado de carregamento que bloqueie a modal. Teste: nenhuma leitura de figura consolidada depende de rede. **A sugestão externa é MAY na spec e ficou não exercida**: o projeto não tem cliente HTTP algum (só Firebase), e trazer um provedor de cotações traz chave de API, licença de dado e um eixo de falha novo — escopo próprio. O slot é o *placeholder* do campo, que por ora carrega a última taxa que o **próprio app** conhece daquela moeda, com a data no `supportingText`. O teste existe e é `ConsolidationBoundaryTest.no consolidated figure depends on the network`, com a lista dos sítios que legitimamente alcançam serviço — nenhum deles produz figura.
- [x] 6.11 **Remoção** de taxa na tela de taxas (D27), com o `ExchangeRateDao` expondo o `delete`. É o corolário obrigatório de a taxa sobreviver à sua origem: sem ele, uma taxa colhida de uma operação já apagada fica sem caminho que a alcance. Teste: removida a única taxa de uma moeda, as figuras que dependiam dela voltam a exibir aquela moeda como termo próprio, em vez de convertida.
- [x] 6.12 Strings de §6 em `values/` e `values-en/`.

## 7. A perna primária e a exibição

- [x] 7.1 `Transaction.primaryEntry` e `Ledger.sourceLeg()` passam a nomear a perna monetária **negativa**, preservando o caso sem perna negativa (compra em cartão). Não é correção de defeito — `min` já devolve a negativa (D16) —, é remoção de dependência tácita, então o teste que prova a mudança é o de **compra em cartão** e o de duas pernas de mesmo sinal, não o de transferência cruzada. Revisar os consumidores de `primaryEntry`/`sourceLeg`.
- [x] 7.2 `AccountUi` e `TransactionUi` passam a carregar a moeda, alimentada pelos mappers a partir da conta — sem isso o formatador não a recebe. **Satisfeita por absorção em 1.1, e a diferença é uma melhoria, não um atalho:** a tarefa imaginava um campo `currency` no modelo de UI, e 1.1 pôs a moeda **dentro** da figura, no `Denomination` de cada `DisplayAmount`. Os 6 campos monetários de `AccountUi` nascem de `Denomination.exact(account.currency)` (`AccountsViewModel:97`) e o único de `TransactionUi` de `leg.currency` (`ItemDisplayAmount:39`), que é a moeda da conta onde a perna postou. Um campo próprio ao lado seria uma segunda fonte da mesma denominação, livre para divergir — exatamente o modo de falha que o KDoc de `DisplayAmount` declara impossibilitar. O `currency: String` que ainda sobra no parâmetro de `AccountCard` serve só à variante `Dashboard`, cujo valor é `Double` cru, e é 7.3 quem o remove.
- [x] 7.3 Trocar as APIs públicas de componente que recebem `Double` cru por valor já denominado: `BalanceCard`, `CreditCardCard`, a variante de dashboard de `AccountCard`, `CategorySpendingCard`, `TotalBalanceCard`. **Cada componente recebeu o tipo final, não um intermediário:** `MoneyFigure` onde a figura agrega entre contas e pode ganhar um termo que a taxa não alcança (`BalanceCard`, `TotalBalanceCard`, `CategorySpendingCard`), `DisplayAmount` onde é monomoeda por construção (`AccountCard.Dashboard`, `CreditCardCard`). Denominar só metade deixaria o `currency: String` avulso vivo para a outra, que é o valor viajando à parte da sua moeda — então `InvoiceUi` e `CreditCardUi.limit` entraram junto, e os parâmetros `currency` dos dois componentes saíram. `CategorySpending` é modelo de domínio e não pode carregar figura de exibição, então nasceu `CategorySpendingUi` (`:core:ui`, ao lado de `InvoiceUi`). **`BudgetProgressCard` ficou de fora de propósito:** D20 o classifica como superfície de degradação declarada, e 7.6 lhe dá o contrato inteiro de uma vez — tocá-lo aqui seria tocá-lo duas vezes.
- [x] 7.4 `MoneyInputTransformation` recebe a moeda, e o default `CurrencyFormatter()` do construtor sai — é a segunda porta de escape para o locale do dispositivo. Passar a moeda nos **11** modais que a usam, incluindo a troca do símbolo quando a conta muda com o campo já preenchido (`TransferBetweenAccountsModal`, `ConfirmRecurringModal`). **A moeda e o fim do default vieram em 1.3;** o que faltava era a segunda metade, e ela não sai da assinatura: uma `InputTransformation` só roda *na digitação*, então trocar a conta sob um campo já preenchido deixava o símbolo antigo até a próxima tecla — o mesmo erro de legenda, alcançado parado. `rememberMoneyInputTransformation` passou a exigir o `TextFieldState`, e sempre que a moeda muda o que está digitado é relido como centavos e reescrito na nova: o valor do usuário não é tocado, só a denominação. Exigir o estado em vez de oferecê-lo é o que impede um 12º campo de nascer imune. A regra mora em `reformat`, pura e testada em `MoneyInputTransformationTest`; a ligação com o Compose não é testável aqui, porque o projeto não tem infraestrutura de teste de composable.
- [x] 7.5 Renderização multitermo como regra única (D22): termos empilhados, uma linha cada, alinhados à direita, o primeiro no estilo da superfície e os demais um degrau abaixo em `onSurfaceVariant`, com o `+` colado ao termo. Nenhuma superfície decide por conta própria. Cobrir com teste de duas e de um termo. **Executada antes de 7.3**, invertendo a ordem dentro do grupo: o renderizador (`MoneyFigureText`, `:core:designsystem`) não depende de nada de 7.3 e é testável hoje — `MoneyFigure.of(listOf(…))` fabrica dois termos sem consolidação alguma —, enquanto a ordem inversa deixaria, ainda que só dentro do grupo, um parâmetro `MoneyFigure` renderizando `primary` sozinho. **O `+` tem uma regra que a decisão não previa:** um termo cuja política já soletra a direção (`-R$ 100,00` de uma despesa) leria `+-US$ 50,00`, então o operador de justaposição só entra onde o termo não traz o seu — o sinal próprio *é* o operador. Isso é derivado da política e do valor em `DisplayAmount.spellsOwnSign`, que é o único ponto onde os dois são conhecidos, e a composição do texto mora em `CurrencyFormatter.formatTerms`, testável sem Compose e reusável pela superfície que não empilha.
- [x] 7.6 Degradação declarada (D20) em `InstallmentCounter`, no rótulo de `BudgetProgressCard` e no medidor de `CreditCardCard`: só o termo na base, com a marca e a indicação de parcela não convertida; nunca truncar nem quebrar. Teste de que nenhum termo é descartado em silêncio. **Duas das três se declaram pelo tipo, e a terceira degrada de fato.** D21 já registra que contador de parcelas e medidor de limite são monomoeda por construção (D17): recebem `DisplayAmount`, o que torna *inexprimível* entregar-lhes duas moedas — não há termo a descartar, e é o compilador quem diz. (`InstallmentState` trocou o par `total: Double` + `currency: String` por um `DisplayAmount`, senão a declaração seria meia.) Só o rótulo de `BudgetProgressCard` degrada: o gasto agrega entre contas e o limite tem uma moeda só, e a gramática é "gasto / limite". **A marca precisou de mais que o `≈`:** sem taxa alguma, a consolidação devolve dois termos *exatos*, e mostrar um deles perderia o outro sem marca nenhuma — então `CurrencyFormatter.formatSingleLine` **força** o `≈` e acrescenta ` +…`, marcador de continuação com o termo elidido, colado ao mesmo `+` de justaposição de D22. Descartados: sufixo localizado (não cabe a 13sp, e forçaria `UiText` num formatador que devolve `String`) e contagem `+1` (número que se lê como dinheiro). **Consequência a registrar para 7.9:** o rodapé que explica a marca precisa perguntar `isApproximate || !isSingleTerm`, não `isApproximate` — senão a marca forçada aparece sem explicação. `BudgetProgressUi` nasceu em `:core:ui` ao lado de `CategorySpendingUi`; a moeda do limite ainda é a única do app e 8.5 troca a fonte. Testes: os casos de linha única em `MoneyFigureTest` e o `DeclaredDegradationTest` (`app/shared/jvmTest`), que afirma sobre as fontes que só a declaração lê os termos de uma figura — hoje com uma exceção nomeada, `ReportExportLayout`, cuja remoção é a prova de 8.1.
- [x] 7.7 Eliminar as duplicatas locais que contornam `DisplayAmount`: `formatMoney` em `EditInvoiceBalanceModal:217` e `EditAccountBalanceModal:229` (ambas prependem `"-"` à mão, reimplementando a política de sinal fora do tipo) e os `parseMoneyToDouble` de `EditInvoiceBalanceModal`, `EditAccountBalanceModal` e `AdvancePaymentModal`. **Os três `parseMoneyToDouble` eram cópias byte a byte de `String.moneyToDouble()` (`:core:common`), o leitor canônico já usado em ~15 sítios — apagados. O `formatMoney` não foi para `DisplayAmount`, e a diferença importa:** este texto é **entrada**, não exibição — ele é relido por `moneyToDouble`, que decide o sinal por `startsWith("-")`, enquanto `DisplayAmount.natural` delega o negativo ao `NumberFormat` da plataforma (o seu próprio KDoc registra isso como decisão à parte). Num locale que ponha o negativo em parênteses ou à direita, semear pelo `DisplayAmount` faria o campo ler de volta o saldo com o sinal trocado, e a primeira tecla o reescreveria diferente da semente. O dono virou `CurrencyFormatter.moneyInput(cents, currency)`, ao lado de `moneyToDouble` em `MoneyFormatter.kt` — os dois sentidos da **mesma gramática**, e `MoneyInputTransformation` (que tinha a terceira cópia, privada) passou a delegar a ele. `MoneyFormatterTest` fixa o round-trip.
- [x] 7.8 Repetir a verificação de 3.10 **no nível de superfície**: com uma conta de moeda diferente da base, o card de conta, a lista de lançamentos, a modal de fatura e o contador de parcelas exibem o símbolo daquela conta, e o da base não aparece em nenhuma dessas figuras. **Mora em `SurfaceDenominationGateTest` (`app/shared/jvmTest`)**, o único módulo que enxerga os três `impl` envolvidos, sobre o grafo Koin real e um banco real: a cláusula negativa é conjunta ("o da base não aparece em **nenhuma**") e espalhá-la por módulo a dissolveria. **Duas das quatro superfícies exigiram fechar antes a metade de leitura do grupo B do inventário:** a modal de fatura e o contador de parcelas são denominados pelo cartão, e `CreditCard` não tinha moeda. Ela veio como **espelho** da linha `LIABILITY` que o cartão é fachada de — mesmo veículo do `isArchived`, pelo `JOIN` que o `CreditCardDao` já fazia —, e não como escolha: 9.7 continua dona do seletor e da escrita, e a inversão de 9.8 segue valendo, porque espelhar não é escolher. Sem isso o teste afirmaria sobre `InvoiceUi`/`InstallmentState` o que `DisplayAmount` já garante, verde enquanto a produção denominava pela base — um anti-gate. **Dois defeitos de infraestrutura de teste apareceram no caminho e estão corrigidos:** sobrescrever o *builder* do banco não funciona (`databaseModule` o traz por `includes`, que o Koin aplica **depois** dos módulos da lista), então `AppModulesTest` vinha abrindo `~/.finance/finsight.db` — o banco real do usuário; e um container Koin não fechado deixa os `single` cacheados **dentro dos `Module`** de `appModules`, que são globais, entregando ao teste seguinte o banco do anterior.
- [x] 7.9 Rodapé de card (D21/D25) no idioma do `helperText`, renderizado só quando alguma linha do card é aproximada, no padrão condicional de `AccountCard:199-213`: explica a marca, revela a taxa usada com a sua data e navega para a tela de 6.5. Teste de que não aparece quando tudo é exato. **"A taxa usada" não existia em lugar nenhum, e a diferença entre revelá-la e reconsultá-la é o ponto:** `ConsolidateFigureUseCase` conhecia a cotação e a descartava a um `?.rate` de distância, jogando fora justamente a **data** que a tarefa pede. Um ViewModel que perguntasse de novo ao repositório tomaria uma **segunda** decisão sobre qual cotação governa aquele número, e duas decisões divergem — a figura reduzida a uma cotação, o rodapé explicando-a com outra. Então a taxa passou a viajar **dentro** da figura: `AppliedRate` (`:core:common`) guardado no `Denomination.approximate`, que deixou de aceitar ser aproximado sem dizer por quê, e derivado em `MoneyFigure.appliedRates` pelo mesmo desenho de `isApproximate`. **A condição do rodapé é `explanationIsOwed(figures)`**, função pura em `:core:common`, e não um `if` no composable: ela reconcilia a marca com a marca *forçada* de 7.6 (`isApproximate || !isSingleTerm`), e é o que torna "não aparece quando tudo é exato" um fato sobre uma função e não sobre um pixel — o projeto não tem teste de composable. **A navegação entra por `LocalOpenExchangeRates`** (`:core:designsystem`, sem default, provido pelo `App`): nenhum `:core:` nomeia feature, e enfiar um callback por 13 sítios de chamada para alcançar um rodapé invisível em quase todos poria o custo do caso raro sobre todos os comuns. Ligado a `BalanceCard`, `CategorySpendingCard` e `TotalBalanceCard` — os três que já recebem `MoneyFigure`. Strings em `values/` e `values-en/`.

## 8. O relatório e o fechamento do inventário

- [x] 8.1 Modelos do documento exportado (`ReportLayout`, `ReportSummaryItem`, `CategoryItem`, `TransactionItem`): hoje guardam **string já formatada** e recebem um formatador único. Acrescentar a marca — textual, e por isso sobrevive à ausência de cor — e a nota de rodapé por família de figura. **A marca veio de graça e o rodapé precisou de forma:** todo valor de célula passa por `CurrencyFormatter.formatSingleLine`, que já força o `≈` e o ` +…`, e com isso `ReportExportLayout.kt` **saiu** da lista de exceções do `DeclaredDegradationTest` — a lista com uma entrada só é a prova desta tarefa. A nota é **por família** (`ReportLayout.summaryFootnote` e `footnote` na interface selada `ReportLayoutSection`), e não uma lista no fim do documento: num meio impresso e sem navegação, a explicação precisa estar adjacente à marca, e cada família deve ou não deve a sua independentemente. A condição é `explanationIsOwed`, reusada e nunca reimplementada — perguntar só `isApproximate` imprimiria a marca *forçada* sem nota. `null` significa "nada devido", de modo que o documento de um usuário monomoeda é byte a byte o de antes. A taxa entra por lambda (`ReportExportStrings.approximationRate`), resolvida na UI: o `:core:model` não conhece `AppliedRate`.
- [x] 8.2 Ligar à consolidação de 5.4 os consumidores que exibem figura consolidada — dashboard, resumo de transações, relatório, orçamentos, categorias —, sem nenhuma multiplicação por taxa em tela, ViewModel ou modelo de UI. Teste de inspeção. **O teste de inspeção ganhou uma cláusula que a tarefa não previa e que é a que importa:** "nenhuma multiplicação por taxa" já era afirmada pelo `ConsolidationBoundaryTest`; o que faltava era *quem lê a moeda base*. `current()` tem um uso legítimo (decidir uma vez — pré-selecionar a moeda de uma conta nova) e um ilegítimo idêntico à vista (uma figura resolvendo a preferência dentro de si), e enquanto a v1 não oferece trocar a base **os dois se comportam igual** — nenhum teste de comportamento pega. Então os leitores são nomeados: as duas telas de configurações, os dois caminhos de pré-seleção e a fábrica de previews, nenhum deles produzindo figura. Três decisões que a spec não fixava, tomadas e registradas: (a) **a data** que governa a consolidação é o fim do período **limitado a hoje**, com dono único em `consolidationDateOf` (`:core:model`) — o fim do período é o que mantém dezembro parado (D11), e o *clamp* é o que impede uma taxa datada no futuro, que o `DatePickerModal` da tela de taxas torna construível, de governar o mês corrente; `today` é insumo de uma emissão, nunca amostrado figura a figura. (b) **O escalar** que ordenar e fracionar exigem não sai de `MoneyFigure` — a consolidação passa a devolver `ConsolidatedFigure(figure, comparable, isPartial)` (`:core:common`), uma decisão com duas projeções, e `comparable` é *o termo líder como ele lê*, política de sinal já aplicada, de modo que número e texto não divergem; `isPartial` carrega a incompletude para dentro de toda fração derivada dele, em vez de deixá-la implícita. Isso mantém `MoneyFigure` sem aritmética (D10) e não abre porta nenhuma ao `DeclaredDegradationTest`. (c) **A fiação** é *mecanismo por construtor, preferência por parâmetro*: `ConsolidateFigureUseCase` entra por construtor, a moeda base entra por argumento e só nasce no `combine` dos ViewModels — esquecer de seguir a preferência deixa de ser tela morta e passa a ser erro de compilação. **Um caso a mais apareceu no caminho e virou regra:** uma leitura agrupada devolve linha por moeda que ela *tocou*, então um perímetro cujas pernas em dólar se cancelam volta com `USD: 0` ao lado dos reais — a consolidação descarta moeda de contribuição nula quando outra sobrevive, senão uma figura exata viraria dois termos e ganharia marca sem ter havido conversão.
- [x] 8.3 Atualizar `TransactionItemSignTest`, `TransactionPerspectiveTest`, `TransactionFormCoherenceTest` e os testes de formatação de `core/common`, `core/ui` e `report`. **A adaptação mecânica já tinha vindo com 1.1; o que faltava era o caso que só existe agora.** As suítes estavam todas em BRL, e com todas as contas na base "denominar pela perna" e "denominar pela base" produzem *o mesmo texto* — a violação de D29 é literalmente invisível ali. Entraram os dois casos que a tornam observável: um lançamento em conta USD lendo em USD (`TransactionItemSignTest`), e **cada ponta de uma transferência cruzada lendo na moeda da sua própria conta** (`TransactionPerspectiveTest`), com as pernas de conversão fora das duas perspectivas e sem transformar `TRANSFER` em `ADJUSTMENT`.
- [x] 8.4 Strings de §7 e §8 em `values/` e `values-en/`: nomes de moeda, explicação da figura aproximada, aviso de parcela não convertida. **Duas das três viraram string e a terceira não podia virar.** A explicação da figura aproximada tem duas formas, porque tem dois meios: `approximation_footer_*` (7.9) para o rodapé que navega, e `report_export_approximation_*` (8.1) para o documento impresso, que não tem para onde navegar e por isso **data** a taxa em vez de sinalizar que ela envelheceu. Mais as do formulário de orçamento (8.6). **Os nomes de moeda não viraram strings, deliberadamente:** uma tabela à mão seria uma entrada por moeda oferecida **por idioma**, mantida em sincronia à mão, enquanto toda plataforma já embarca o dado ICU que responde exatamente isso — e responde para idiomas que o app não tem recursos. Nasceram `currencyDisplayName`/`currencySymbol` (`expect`/`actual` nos três alvos, ao lado de `deviceCurrencyCode`), com recuo para o próprio código, que nunca está errado e é o que um leitor que conhece o código espera. **O aviso de parcela não convertida não é string por decisão de 7.6:** ele é o marcador de continuação ` +…`, e o sufixo localizado foi descartado ali por não caber a 13sp e por forçar `UiText` num formatador que devolve `String` — traduzi-lo agora seria desfazer aquela decisão pelas costas dela. As strings de §9 são de 9.9.
- [x] 8.5 **Moeda do limite de orçamento** (D13): a entidade de orçamento ganha o campo de moeda (a coluna e o preenchimento vêm de 4.2); `CalculateBudgetProgressUseCase` reduz o gasto à **moeda do limite**, não à base. **Sem redutor novo:** a redução à moeda do limite é o mesmo `ConsolidateFigureUseCase`, com `base = budget.currency` — 8.5 virou um argumento e não uma máquina. `BudgetProgress.spent` passou a `ConsolidatedFigure`, e `progress`/`remaining`/`isExceeded` leem o `comparable` dele, com `isApproximate` ao lado para que a superfície não reconstitua a pergunta.
- [x] 8.6 `BudgetFormModal`: o controle de moeda **só existe quando há mais de uma moeda entre as contas cadastradas**, pré-selecionado com a moeda da **conta padrão**; com uma só, nenhum controle é exibido e o limite assume a moeda da conta padrão. Na edição a moeda é apresentada travada. Comentar por que difere de D23 — o formulário de conta é a porta pela qual uma moeda nasce e precisa estar sempre aberta; o de orçamento só escolhe entre as que existem. **As moedas oferecidas são as das contas cadastradas**, nunca o catálogo e nunca a base: um orçamento não denomina nada novo. A submissão de uma edição **não** grava `state.currency`, pela razão de D13 — reinterpretar um limite já digitado é reescrever o que ele significava.
- [x] 8.7 Teste do `BudgetFormModal` nos dois perfis: uma moeda cadastrada → **nenhum controle**, limite na moeda da conta padrão; duas moedas → controle presente, pré-selecionado na conta padrão mesmo quando ela difere da base. **Afirmado sobre o estado, não sobre o pixel** — o projeto não tem infraestrutura de teste de composable, e é no `BudgetFormUiState` que a decisão mora. `BudgetFormCurrencyTest` cobre os dois perfis, mais dois casos que a tarefa não pedia e que fecham a regra: a escolha do usuário vence a sugestão, e **a edição mostra a moeda do próprio orçamento, travada** — nem a da conta padrão nem a base, porque reinterpretar um limite já digitado é reescrever o que ele significava.
- [x] 8.8 Teste do progresso no perfil que motiva a regra: todas as contas em moeda diferente da base, limite na moeda das contas, gasto inteiramente nela → **progresso exato, sem marca**. E o perfil oposto: contas em duas moedas, gasto em ambas → progresso aproximado, com marca. **O primeiro caso tem uma taxa de dólar cadastrada de propósito**, porque é ela que o desenho anterior teria acionado: com uma moeda só não havia nada a reconciliar, então a taxa não participa e a barra continua exata. Um terceiro caso entrou porque o tipo o tornou exprimível: gasto em duas moedas **sem taxa** — a barra divide só o que as taxas alcançaram, `isPartial` carrega isso para dentro da fração, e a figura ao lado continua com os dois termos, sem nada inventado. Mora em `BudgetProgressCurrencyTest` (`:feature:budgets:api`), e no caminho o `MonthBalances` do arquivo vizinho virou a base por módulo de 3.7 (`StubEntryRepository`), que **recusa** toda leitura não estubada em vez de responder zero.
- [x] 8.9 Verificar que os cenários de `dashboard-balance-widgets` e `transaction-scope` estão cobertos por teste, inclusive o do pagamento de fatura cruzado permanecendo interno ao perímetro neutro. **`transaction-scope` estava inteiro** — os cinco cenários, incluindo os dois cruzados, vieram em 3.15. **`dashboard-balance-widgets` faltava dois**, e ambos entraram em `DashboardOverallBalanceStatsTest`: o pagamento de fatura cruzado não é despesa pela mesma razão que o monomoeda não é (as duas pernas **monetárias** estão dentro; as de conversão estarem fora não as torna fluxo), e a soma do perímetro neutro soma cada moeda com a sua e não converte nada — com contas em duas moedas e sem taxa, a figura fica com os dois termos, provando que nenhuma conversão participou da *derivação*. O cenário "sem agregado dedicado" continua sendo estrutural: a soma é `CurrencyBalance.plus`, do razão, e não uma consulta criada para o perímetro.
- [x] 8.10 **Fechar a lista de 1.2** (`inventory-assumed-single-currency.md`): nenhum sítio de produção passa a moeda base por não saber qual é a moeda da figura. O critério de pronto é mecânico e não uma auditoria — **apagar `ASSUMED_SINGLE_CURRENCY` de `:core:model` e compilar**: se compila, a lista está vazia. É por isso que a constante existe em vez de só um documento, e é o que impede que o provisório sobreviva. **Fechada.** A constante e o arquivo `AssumedSingleCurrency.kt` foram apagados e o projeto inteiro compila — o que a lista pedia. Os três sítios que restavam eram os dois do formulário de cartão e o *default* de `CreditCard.currency`, todos territórios de 9.7: o formulário passou a escolher a moeda, `CreditCardForm` a carregá-la e `CreditCard.currency` perdeu o default, como `Account.currency` já não tinha. O `CreditCardMapper` passou a exigir a moeda por parâmetro em vez de recair num default — o cartão é fachada de uma linha `LIABILITY`, e é de lá que a moeda vem.

## 9. Os fluxos de dois valores e a porta

> A partir de 9.2 a segunda moeda passa a existir. Os fluxos vêm **antes** dela de propósito:
> aberto o seletor, o usuário cria uma conta em USD e tenta transferir no minuto seguinte, e
> sem 9.1 a transferência bate na guarda de resíduos e é recusada — feature meio entregue.

- [x] 9.1 Os três fluxos de dois valores: `TransferBetweenAccountsUseCase` aceita o valor de destino; `PayInvoicePaymentUseCase` ganha um valor de entrada que **hoje não existe** (é derivado e exibido somente-leitura — o campo do devido mantém o papel, o editável é novo, abaixo dele), com `Action` e `UiState` novos; `AdvanceInvoicePaymentUseCase` ganha o par, com o teto `amount <= currentBillAmount` passando a valer sobre o campo na **moeda do cartão**. Nos três modais: segundo campo por `AnimatedVisibility`, campos nomeando a conta, taxa derivada como `supportingText`, e o `enabled` do botão cobrindo o **segundo** campo — sem isso a guarda de 2.8 fica alcançável por valor zerado. **Feita, com um componente para a gramática comum.** Os três valores segundos entram sem *default* (`destinationAmount`, `accountAmount`): no caso monomoeda o chamador passa o mesmo número duas vezes, e no cruzado esquecê-lo é erro de compilação em vez de figura errada. **A igualdade dos dois valores quando as moedas coincidem não é revalidada** — `Σ = 0` por moeda tem um dono, o escriturador, e reimplementá-la aqui seria a duplicação que o projeto proíbe. O segundo campo virou `CrossCurrencyAmountField` (`:core:designsystem`), porque a taxa derivada, a cotação oferecida e a regra de quando ela pode ser digitada são a **mesma** decisão em três telas e dois módulos `impl` que não se enxergam. O `enabled` dos três botões cobre o segundo campo — sem isso a guarda de 2.8 fica alcançável por valor zerado.
- [x] 9.2 Pré-preencher o segundo valor **apenas** quando a taxa conhecida é do mesmo dia; fora disso, placeholder com a data no `supportingText`. Não é conveniência: o valor digitado vira taxa colhida, e pré-preencher com cotação velha a regravaria como nova, em laço. **Feita, e a regra tem dono próprio:** `SuggestConvertedAmountUseCase` (`:core:model`) devolve `ConversionSuggestion(amount, rate, isFromOperationDate)`. A comparação `rate.date == data da operação` é regra de domínio, não de apresentação — é ela que impede que o valor deixado no campo, que **vira** taxa colhida, regrave uma cotação velha como nova. O que fazer com a resposta (preencher vs. *placeholder* datado) fica no componente. **Só um campo vazio é semeado:** o que o usuário digitou nunca é sobrescrito por uma sugestão que chega depois, nem quando ele muda a data.
- [x] 9.3 Colher a taxa de toda operação cruzada — derivada das duas pontas, na data da operação, origem de operação — a partir do caminho de escrita da feature, nunca do razão. A taxa **sobrevive** à remoção da operação que a originou (D27), então a remoção de transação não a toca. Testes: a operação gravada não possui campo de taxa; apagar a operação não altera a taxa nem as figuras do período. **Feita.** `CollectOperationRateUseCase` (`:core:model`, ao lado da consolidação e pela mesma razão: dois `impl` que não se veem) deriva a cotação das duas pontas e a grava com origem de operação. Três casos não colhem nada, e nenhum é erro: mesma moeda, **nenhuma das pontas sendo a base** (Non-Goal de D11 — não há matriz de pares) e ponta não positiva. **A colheita acontece depois da escrita, em `catch {}`, e não pode desfazê-la:** a operação é o fato, a taxa é o que ela ensinou, e D27 já separa os dois ciclos de vida — recusar uma transferência válida porque o `INSERT` da taxa falhou inverteria a hierarquia. Testes em `OperationRateTest` (`:core:model`) e, de ponta a ponta, em `CrossCurrencyOperationTest` (`app/shared/jvmTest`), que apaga a transferência e prova que a taxa continua lá.
- [x] 9.4 (D17, D26) `ConfirmRecurringUseCase` recusa com erro tipado redirecionar para conta ou cartão de outra moeda (D17); `ConfirmRecurringModal` oferece apenas os da moeda da recorrência **e diz por que a lista encolheu**. Exibir valor com a moeda correta em `RecurringFormModal`, `ViewRecurringModal` e `RecurringScreen`. **Feita.** `RecurringError.CURRENCY_MISMATCH` com `toUiText()` e strings nos dois idiomas; `ConfirmRecurringUseCase` recusa antes de montar a intenção, tanto no caminho de conta quanto no de cartão. O `ConfirmRecurringViewModel` filtra contas e cartões pela moeda da recorrência e expõe `hiddenByCurrency`, e o modal diz por que a lista encolheu — uma lista silenciosamente mais curta é mentira por omissão (D26). O ViewModel passou a exibir o erro **tipado** em vez do genérico. A exibição do valor na moeda certa já vinha do grupo 8 (`Recurring.currency` derivada). Teste: `ConfirmRecurringCurrencyTest`.
- [x] 9.5 **Recusa de domínio incondicional** para a moeda: `UpdateAccountUseCase` e o caminho de atualização do cartão recusam qualquer alteração de moeda, **sem consultar `hasEntries` nem estado algum** (D12). A spec exige que a tentativa seja recusada pelo domínio; sem isto a regra viveria só na UI, que é a inversão que o projeto proíbe. Teste: uma atualização que altere a moeda é recusada tanto numa conta com lançamentos quanto numa sem. **Feita, e incondicional nos dois caminhos:** `UpdateAccountUseCase` e `UpdateCreditCardUseCase` recusam qualquer alteração de moeda **sem consultar `hasEntries` nem estado algum**, com `AccountError.CURRENCY_IMMUTABLE` e `CreditCardError.CURRENCY_IMMUTABLE`. `AccountCurrencyImmutabilityTest` prova os dois perfis — e a ausência de `hasEntries` no teste é parte da afirmação.
- [x] 9.6 `CurrencyPickerModal` em `core/designsystem`, irmão do `IconPickerModal`, alimentado pelo catálogo de 5.2. **Antecipado por 8.6, que precisava de um controle.** A lista entra por **parâmetro**, exatamente como a do `IconPickerModal`: nenhum componente de `:core:designsystem` alcança `:core:model` para buscar o catálogo, e isso deixa um chamador oferecer um conjunto *mais estreito* que ele — o formulário de orçamento oferece só as moedas em que o usuário de fato tem conta. Cada linha diz três coisas (código, glifo e nome no idioma do leitor) porque nenhuma delas sozinha identifica uma moeda para todo mundo.
- [x] 9.7 **A porta.** Linha de moeda **sempre visível** no `AccountFormModal`, reusando inteiro o `DefaultAccountSelector:207-290` — símbolo como glifo na caixa de 52dp, subtítulos alternativos, caixa de `primary` → `onSurfaceVariant` e chevron ausente quando travada. **Seletor no formulário de criação, pré-selecionado com a moeda base; estado travado no de edição, sempre** — decidido pelo modo do formulário, não pelo estado da conta (D12). Idem no `CreditCardFormModal`, com `CreditCardRepository:57` gravando a moeda escolhida. Sufixo `· US$` no `AccountSelector`/`CreditCardSelector` quando houver mais de uma moeda. **Feita.** `CurrencySelectorRow` (`:core:designsystem`) reusa a forma do `DefaultAccountSelector` inteira — caixa de 52dp com o **símbolo** no lugar do ícone, subtítulo alternativo, `primary` → `onSurfaceVariant` e chevron ausente quando travada — e mora no design system porque os **dois** formulários a usam, o que também é o que torna 9.8 contável. Sempre visível; seletor na criação, travada na edição, decidido pelo modo do formulário. `CreateAccountUseCase` deixou de ler a base e passou a receber a moeda escolhida; `CreditCardRepository.insert` grava `creditCard.currency` em vez da base, e o `CreditCardForm` carrega a escolha. Sufixo `· US$` no `AccountSelector`/`CreditCardSelector` **quando a própria lista tem mais de uma moeda** — o que um seletor precisa desambiguar é o que ele está mostrando, e uma lista estreitada a uma moeda (a da confirmação de recorrência) já diz isso com palavras.
- [x] 9.8 **Inverter** o teste de inércia de 1.6: existem exatamente **dois** sítios de produção que escolhem a moeda de uma conta — `AccountFormModal` e `CreditCardFormModal` —, e qualquer terceiro falha o teste. **Feita, e o critério mudou de forma junto com a regra.** O teste virou `CurrencyChoiceSitesTest`: conta os arquivos de produção que usam `CurrencySelectorRow` e exige exatamente dois. **O `CurrencyPickerModal` não serve de âncora** — o formulário de orçamento também o abre, e legitimamente: ele escolhe entre as moedas que já existem e não denomina nada novo (D13). A segunda metade do teste antigo sobreviveu inteira: toda construção de conta ou recebe a moeda por argumento ou copia a linha que converte, e nenhuma a escolhe.
- [x] 9.9 Strings de §9 em `values/` e `values-en/`. **Feita, ao longo do grupo.** Rótulos dos campos que nomeiam a conta, explicação da lista encolhida da recorrência, os dois erros de moeda imutável, o rótulo e o estado travado do seletor de moeda e o título do seletor. Paridade verificada mecanicamente: `values/` e `values-en/` têm exatamente as mesmas 701 chaves.

## 10. Fechamento

- [x] 10.1 Verificação de ponta a ponta: criar conta em USD, transferir de BRL, pagar de conta BRL a fatura de um cartão USD. Conferir as 4 entries somando zero por moeda, a conversão sem dimensão, o rótulo `TRANSFER` e `PAYMENT` (não `ADJUSTMENT`), a taxa colhida na data, o patrimônio com `≈` e rodapé, e o saldo de conta e a fatura exatos. **Feita, e mora em `CrossCurrencyOperationTest` (`app/shared/jvmTest`)** — grafo Koin real, banco real, os dois fluxos. A transferência BRL→USD é escrita com 4 pernas somando zero em cada moeda, uma conta `CONVERSION` **por moeda** e nenhuma delas com dimensão; o rótulo é `TRANSFER`, não `ADJUSTMENT`; a taxa 5,50 é colhida na data da operação com origem de operação; e **apagar a transferência não a remove** (D27). O pagamento de fatura de cartão USD a partir de conta BRL lê como `PAYMENT`, mantém a dimensão da fatura só na perna `LIABILITY`, quita exatamente os US$ 100 devidos e colhe a mesma cotação. **A moeda base é ligada por módulo de teste** em vez de resolvida do locale: se ela dependesse da região da máquina, a colheita — que exige uma ponta na base — não aconteceria em CI fora do Brasil e o teste passaria sem afirmar nada. O `≈` e o rodapé de patrimônio são de 7.9, cobertos por `MoneyFigureTest` e pelos gates de denominação.
- [x] 10.2 Rodar 3.9 uma última vez: perfil só-BRL sem marca em superfície alguma. **Rodado.** `CurrencyDenominationGateTest` (3.9/3.10), `SurfaceDenominationGateTest` (7.8), `DeclaredDegradationTest` e `ConsolidationBoundaryTest` passam junto com o resto: `./gradlew jvmTest testDebugUnitTest --rerun-tasks` verde de ponta a ponta.
- [x] 10.3 Atualizar `core/ledger/README.md` — referência normativa do módulo — com a conta de conversão, a moeda da perna vinda da conta e as leituras por moeda. **Feito.** O conjunto de tipos, a `currency` sem default da `Account`, a `CONVERSION` e por que ela não é `EQUITY`, as contas de sistema **por moeda** resolvidas por `(type, name, currency)`, a moeda da perna vinda da conta (e `TransactionLeg` sem campo), as leituras por moeda com as três escalares nomeadas, e o passo de completação cruzada na lista do que a fronteira de escrita faz. `netWorth()` saiu da tabela de leituras junto com a interface (3.6).
- [x] 10.4 Atualizar `CLAUDE.md`: o conjunto de tipos de conta deixa de ser de cinco membros e as contas de sistema deixam de ser "apenas três". **Feito.** Os três pontos do bloco *Ledger*: o conjunto de tipos com `CONVERSION` e a razão de ela não ser `EQUITY`; as contas de sistema, que deixaram de ser "apenas três" e passaram a ser quatro espécies **uma por moeda**; as leituras por moeda com a consolidação fora do razão; e a perna sem moeda própria na escrita.
- [x] 10.5 Revisar os Non-Goals e confirmar que nenhum foi implementado por acidente: edição de transação cruzada, taxa entre duas moedas não-base, troca de moeda base, ganho cambial em tela, moeda de expoente ≠ 2. **Revisados, um a um, e nenhum foi implementado por acidente.** (a) *Edição de transação cruzada:* o gate continua contando pernas **monetárias**, e `ViewTransactionGatesTest` o exercita com pernas `CONVERSION` presentes. (b) *Taxa entre duas moedas não-base:* `CollectOperationRateUseCase` retorna sem gravar quando nenhuma ponta é a base, com teste próprio. (c) *Troca de moeda base:* `IBaseCurrencyRepository.set` não tem um só chamador de UI — a tela de configurações lê a preferência e não a escreve. (d) *Ganho cambial em tela:* `AccountType.CONVERSION` não aparece em nenhuma superfície, só no razão e em testes; nenhum seletor ou listagem o alcança, porque todos filtram por `isMonetary`. (e) *Moeda de expoente ≠ 2:* o catálogo curado segue sendo a única fonte do que é oferecido.
- [x] 10.6 **Apagar `inventory-assumed-single-currency.md`** e a referência a ele no cabeçalho deste arquivo. Ele é andaime da implementação, não documento da change: existe para dar backlog aos grupos 3–9 enquanto a moeda de uma figura ainda não é conhecível em todo sítio, e 8.10 já provou — apagando a constante — que não sobrou nenhum. Mantê-lo depois disso descreveria um estado que o código não tem mais. **Feito.** O arquivo e o parágrafo do cabeçalho que o descrevia foram removidos; a menção que resta no texto de 1.2 e de 8.10 é histórica, e é o registro de que a lista existiu e foi fechada.
